name: Deploy Krant AR

on:
  push:
    branches:
      - main
    paths:
      - 'krant/**'
      - '.github/workflows/deploy-krant.yml'
  workflow_dispatch:

jobs:
  web-deploy:
    name: Deploy krant naar /krant
    runs-on: ubuntu-latest
    steps:
      - name: Get latest code
        uses: actions/checkout@v3

      - name: Sync krant to cPanel
        uses: SamKirkland/FTP-Deploy-Action@v4.3.4
        with:
          server: ${{ secrets.FTP_SERVER }}
          username: ${{ secrets.FTP_USERNAME }}
          password: ${{ secrets.FTP_PASSWORD }}
          local-dir: krant/
          server-dir: ./krant/
          exclude: |
            **/.git*
            **/.git*/**
            **/node_modules/**
            data/essays.db
            data/login_attempts.json
            tools/package-lock.json
            .DS_Store
            **/.DS_Store
            docs/**
